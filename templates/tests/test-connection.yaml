apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-big-data-platform.fullname" . }}-test-connection"
  labels:
    {{- include "helm-big-data-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-big-data-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
